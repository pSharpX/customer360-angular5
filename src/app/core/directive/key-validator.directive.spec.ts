import { KeyValidatorDirective } from './key-validator.directive';

describe('KeyValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
